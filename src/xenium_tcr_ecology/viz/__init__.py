"""Shared publication styling for manuscript figures.

`style.py` defines the single typography, colour-role, and layout
convention (colourblind-safe Okabe-Ito palette, panel-labelling,
despined axes) used across every main-text figure builder in
`release/redesigned_main_figures.py` and
`tcr/vdj_ground_truth_validation.py`. It only changes how already-computed
results are drawn -- it does not read, compute, or alter any statistic.
"""
