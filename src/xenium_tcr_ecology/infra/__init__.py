"""Infrastructure utilities shared by every phase script: project-root
resolution, structured provenance logging, inventory writing, and the
project's typed exception for expected/handled failures.

No biological, statistical, or dataset-specific logic lives here -- see
xenium_tcr_ecology.io / .qc / .graphs / .nullmodels / .annotation /
.clone_ecology / .viz for that.
"""
